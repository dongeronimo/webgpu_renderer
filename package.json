{
  "name": "webgpu-volume-renderer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "wgpu-matrix": "^3.4.0"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
