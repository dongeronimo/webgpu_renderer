The package "interfaces" holds the points-of-entry and exit of the server to the rest of the world.

A point-of-entry can be a websocket or a rest endpoint.

While the gameplay must use websockets we'll also need https for signaling.