# Dependências do dicom_converter.py
# Instalar com: pip install -r requirements.txt
numpy>=1.24
pydicom>=2.4

# Opcional — suavização Perona-Malik na GPU (requer CUDA instalado).
# Instale manualmente o pacote correspondente à sua versão do CUDA:
#   pip install cupy-cuda12x   # CUDA 12.x
#   pip install cupy-cuda11x   # CUDA 11.x
# Sem CuPy o conversor funciona normalmente, apenas pula a suavização.
